// write_recipe_view.dart
import 'package:flutter/material.dart';

class WriteRecipeView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Write Recipe Page',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}